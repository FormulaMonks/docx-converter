function map(object) {
  const properties = object.getOwnPropertyNames();
  const mappedObject = {};
  properties.forEach((element) => {
    switch (element) {
        case 'p':
            mappedObject[element] = 
    }
  });
}
